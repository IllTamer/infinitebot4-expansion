plugins {
    id 'java'
    id 'com.github.johnrengelman.shadow' version '8.1.1'
}

version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'com.github.johnrengelman.shadow'

    group 'com.illtamer.infinite.bot.expansion'

    version = rootProject.version

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(25)
        }
    }

    repositories {
        mavenLocal()
        maven {url "https://maven.illtamer.com/releases"}
        maven {url "https://repo.papermc.io/repository/maven-public/"}
       	maven {url 'https://repo.extendedclip.com/content/repositories/placeholderapi/'}
        maven {url 'https://hub.spigotmc.org/nexus/content/repositories/snapshots/'}
        maven {url 'https://repo1.maven.org/maven2'}
        mavenCentral()
    }

    dependencies {
        compileOnly 'com.illtamer.infinite.bot:minecraft:4.2.0-SNAPSHOT'
        compileOnly 'io.papermc.paper:paper-api:1.21.11-R0.1-SNAPSHOT'
        compileOnly 'me.clip:placeholderapi:2.11.7'

        compileOnly 'org.projectlombok:lombok:1.18.42'
        annotationProcessor 'org.projectlombok:lombok:1.18.42'

        testCompileOnly 'com.illtamer.infinite.bot:minecraft:4.2.0-SNAPSHOT'
        testCompileOnly 'io.papermc.paper:paper-api:1.21.11-R0.1-SNAPSHOT'
        testCompileOnly 'me.clip:placeholderapi:2.11.7'
        testCompileOnly 'org.projectlombok:lombok:1.18.42'
        testAnnotationProcessor 'org.projectlombok:lombok:1.18.42'
    }

    shadowJar {
        archiveBaseName = parent.name + '-' + project.name
        version = project.version
        archiveClassifier = 'all'
        manifest {
            attributes('Automatic-Module-Name': 'com.illtamer.infinite.bot.expansion')
        }
    }

    // 项目历史上把 sandbox main() 放在 src/test/java（无 @Test 注解），
    // 只需编译通过而无需真正执行 JUnit，避免 Gradle 将其误当作测试类加载。
    tasks.withType(Test).configureEach {
        enabled = false
    }
}
